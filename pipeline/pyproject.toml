[project]
name = "highlighter-pipeline"
version = "0.1.0"
description = "TransNetV2 shot-detection sidecar for the Highlighter pipeline worker."
requires-python = ">=3.11"
dependencies = ["transnetv2-pytorch", "numpy"]
